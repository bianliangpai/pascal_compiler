## Dependencies

### cmake
  sudo apt-get install cmake

### gtest
  cd /path/to/googletest
  mkdir build
  cd build
  cmake ..
  make
  sudo make install

### pthread
  sudo apt-get install libpthread-stubs0-dev
